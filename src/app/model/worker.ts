export interface Worker {

    _id : string,
    name : string,
    surname : string,
    id : string,
    workingPosition : string, //ceo, manager, forklift driver...
    doy : string, //date of joininig the company
    dob : string, //date of birth
    email: string
}
